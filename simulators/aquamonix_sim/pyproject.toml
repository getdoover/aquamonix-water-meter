[project]
name = "aquamonix-sim"
version = "0.1.0"
description = "Modbus simulator for Aquamonix i500 series flow meter"
requires-python = ">=3.11"
dependencies = [
    "pymodbus>=3.9.2",
    "transitions>=0.9.2",
]
